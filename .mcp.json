{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--browser", "chrome",
        "--user-data-dir", "./.browser-profile",
        "--viewport-size", "1440,1600"
      ]
    }
  }
}
